# Icons

Place 16/48/128 px PNG icons here:

- `icon-16.png`
- `icon-48.png`
- `icon-128.png`

Until real assets are designed, the extension will load with the browser's default
placeholder icon. Generate placeholders with any tool you like (the manifest will
not block the build if these are missing).
